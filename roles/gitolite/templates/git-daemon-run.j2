#!/bin/sh
# {{ ansible_managed }}
exec 2>&1
echo 'git-daemon starting.'
exec chpst -ugit \
  "$(git --exec-path)"/git-daemon --verbose --reuseaddr \
  --base-path=/srv/git/repositories /srv/git/repositories

